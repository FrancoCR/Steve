def main():

    while True:
        # gets audio from mic
        r = sr.Recognizer()
        with sr.Microphone() as source:
            # play audio file
            audio = r.listen(source)
            # play audio file
        decisionTreeCheck(r, audio)
